import { CardDraggingDirective } from './card-dragging.directive';

describe('CardDraggingDirective', () => {
  it('should create an instance', () => {
    const directive = new CardDraggingDirective();
    expect(directive).toBeTruthy();
  });
});
